name: hello1

on:
    push:
        branches:
            - master
    workflow_dispatch:

jobs:
    hello2:
        name: hello2externamname
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: hellow3
              run: echo "Hellow world"
              shell: bash
